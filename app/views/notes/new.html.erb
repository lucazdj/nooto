<%= tag.div id: dom_id(@note, :new), data: { modal_target: "content" } do %>
  <%= tag.div class: "card #{bg_color note.color, 400}" do %>
    <%= render "form", note: @note, type: :new %>
  <% end %>
<% end %>
