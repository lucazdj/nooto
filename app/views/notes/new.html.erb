<%= tag.div id: dom_id(note), data: { modal_target: "content" } do %>
  <div class="card">
    <h1 class="font-bold text-4xl"><%= t(:new_) %></h1>
    <%= render "form", note: @note, type: :new %>
  </div>
<% end %>
