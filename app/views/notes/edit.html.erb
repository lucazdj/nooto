<%= tag.div id: dom_id(@note, :edit), data: { modal_target: "content" } do %>
  <%= tag.div class: "card flex flex-col gap-3 bg-#{ @note.color.blank? ? 'green' : @note.color }-400" do %>
    <%= render "form", note: @note, colors: @colors, type: :edit %>
  <% end %>
<% end %>
