<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <title>Niawu</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="flex flex-col h-screen bg-lime-50">

<header class="h-16 shrink-0 bg-green-300">
  <div class="h-full container flex justify-center items-center">
    <span class="header-logo"><%= link_to t(:app_name), root_path %></span>
  </div>
</header>

<main class="container grow mx-auto mt-16 px-3 flex">
  <%= yield %>
</main>

<footer class="h-16 shrink-0 bg-green-300">
  <div class="h-full container flex justify-center items-center">
    <span class="header-logo"><%= t(:app_name) %></span>
  </div>
</footer>

</body>
</html>
